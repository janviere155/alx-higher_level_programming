#!/usr/bin/python3
"""Module having the ``lookup`` function."""


def lookup(obj):
    """Definition.
    Args:
        obj (object): parameter

    Returns:
        dir(obj)
    """
    return dir(obj)
